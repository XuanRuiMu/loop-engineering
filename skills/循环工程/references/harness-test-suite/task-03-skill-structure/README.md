# 任务 3：验证 skill 目录结构

## 描述
确认循环工程 skill 根目录与 `references/` 目录包含约定的核心文件，并且 `harness-test-suite/` 下至少存在 3 个任务目录。

## 输入
- skill 根目录及其 `references/` 子目录

## 预期输出
- `PASS: skill 目录结构符合规范`

## 验证命令

```bash
python task-03-skill-structure/verify.py
```
