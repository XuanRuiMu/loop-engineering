# 循环工程证据记录（EVIDENCE.md）

## 证据记录格式

| 日期 | 证据 ID | 失败模式 | 错误分类 | 摘要（一行） | 规避/预防措施 |
| --- | --- | --- | --- | --- | --- |
| YYYY-MM-DD | EV-xxx | {失败模式描述} | 鉴权/网络/数据/逻辑/依赖/未知 | {一句话摘要} | {已应用的规避或预防措施} |

## 活跃证据

| 日期 | 证据 ID | 失败模式 | 错误分类 | 摘要（一行） | 规避/预防措施 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-31 | EV-001 | pino v10 `pino.destination()` 异步打开文件，flush 不保证落盘，测试读日志文件得空数组 | 依赖 | 某日志重构 FP-02：debug日志/日志接收测试因文件空全挂，改用 fs.createWriteStream + stream.end(callback) 修复 | 已记录至 BUDGET.md 环境问题规避表；后续测试日志读写必须走直写流方案 |
| 2026-07-31 | EV-002 | PROGRESS.md 熔断计数未同步递增（元信息与熔断表格只改一处） | 逻辑 | 日志重构会话更新 PROGRESS.md 时漏增循环计数，元信息=1 实际完成 3 个 FP | 子代理提示词模板已含一致性检查；主代理更新后必须自检两处计数一致 |
| 2026-08-05 | EV-003 | 子代理证据文件未按 traces/ 子目录与命名规范落位（写到 `.agents/evidence/FP-02.md`） | 逻辑 | 某便签重做 FP-02 的证据文件位置/命名违反 EnvironmentEngineering 结构规范，根因是子代理提示词模板只写"以 .agents/evidence/ 开头"未指明 traces/ 结构 | 主代理已手动归位为 `traces/FP-02-e2e-stale-baselines-20260805.md`；模板修复提案（待确认级）已上报用户 |
| 2026-08-12 | EV-004 | 沙箱 bash 无 TTY，`github.com:443` 间歇性 TCP 重置，子代理裸 git push 卡认证或网络失败 | 网络/鉴权 | 某前端项目三 FP 修复后子代理本地提交成功但裸 git push 失败（TCP reset）；改用项目内推送封装（自带 token 注入）推送 | 本项目 GitHub 上传统一走项目内推送封装（自带 token 注入），禁止子代理裸 git push |
| 2026-08-16 | EV-005 | 补位无上限：多个子代理低质/失败时主代理持续亲自干活，主上下文重新膨胀，撞上 Context Wall（与 Headless 目标对冲） | 逻辑 | 元分析发现『优先子代理执行』铁律与『无 Task 工具时补位非降级』未设主代理亲自补位上限，构成内部矛盾 | 新增 `orchestrator_backfill_limit`（默认3）与补位熔断（BUDGET.md + SKILL.md）；达上限改派全新子代理或停下汇报；无 Task 工具环境下降级为可见风险 |
| 2026-08-16 | EV-006 | 跨功能点契约耦合：A 改公开契约，B（依赖/并行）基于旧契约实现，集成才靠编译错误暴露，浪费双份子代理预算 | 依赖 | 元分析发现依赖验证仅事后编译暴露、并行前编译验证只看派发前基线，看不到并发契约变更 | 新增『契约变更协调』：PROGRESS.md『契约变更』节记录 delta（只写进度铁律的唯一例外）+ 派发时注入消费者 + 并行前契约冲突扫描（SKILL.md + PROGRESS模板 + 子代理提示词模板） |
| 2026-08-16 | EV-007 | Self-Harness 元循环确认偏差：自动级核心提案由主代理既提又验，只过回归（查结构在不在）不过独立语义审查，可能应用语义错误的 harness 改动 | 逻辑 | 严格复审发现元循环三步全由主代理完成；其自身"兼容性"节引用 AGENTS.md P0 却未落实"确认偏差"条，与要求子代理独立上下文避确认偏差的原则自相矛盾 | 自动级核心提案（触及 SKILL/BUDGET/HARNESS）插入"方案审查"快速模式（反对者子代理）独立对抗审查，无致命/重大风险才应用；纯模板/格式类自动提案可跳过 |
| 2026-08-16 | EV-008 | 回归套件假红：task-01 要求存在运行时 PROGRESS.md 才通过，干净仓库仅含模板无运行时实例 → 默认 5/6 | 依赖 | 实跑 run_all.py 得 5/6，临时造 PROGRESS.md 才 6/6；Self-Harness 自身测试套件在干净检出非全绿，削弱"禁止交付未验证产物"约束力 | task-01 无运行时 PROGRESS.md 时改以 PROGRESS模板.md（模板即结构规范）为基准校验，干净仓库默认 6/6 |
| 2026-08-16 | EV-009 | 断点续跑孤儿态：会话在子代理执行中崩溃，半改的"进行中"功能点被当作仍在进行中继续叠加，无 rebaseline | 逻辑 | 断点续跑小节只"识别进行中、从断点继续"，未先重验基线/重置脏态 | 断点续跑加强制 rebaseline：孤儿"进行中"先重跑基线编译/构建/测试，脏则重置为待开始重做；明确不靠"会话交接"（后者解决主动换会话上下文传递，不解决脏工作树） |
| 2026-08-16 | EV-010 | 预算荣誉制：wall_clock/total_turn 依赖主代理自行计时计数，无外部看门狗强制，易被误读为硬停 | 依赖 | BUDGET.md 预算字段为软约束，长任务可能因主代理未精确计数而超出上限 | BUDGET.md 兼容性声明下新增"已知局限"：明确预算为荣誉制软约束、token 维度不可获取，避免幻觉式保证 |

| 2026-08-16 | EV-011 | 文档漂移：Orchestrator-Headless模式.md 引用 PROGRESS.md『循环历史摘要』（被模板压缩规则#3 与铁律明确禁止）且『决策日志』术语与模板『当前决策』/SKILL L172 不一致 | 逻辑 | 严格复审发现架构说明文档与 PROGRESS 模板/铁律术语冲突，子代理按文档找章节会违背铁律 | 删『循环历史摘要』引用，『决策日志』统一为『当前决策』对齐模板权威命名（Orchestrator-Headless模式.md L89/L93） |
| 2026-08-16 | EV-012 | 治理自矛：HARNESS 自动级含"修复笔误"（子代理可直接改），与 EnvironmentEngineering §2.3 harness-test-suite 测试文件须 AskUserQuestion 冲突，子代理遇测试文件笔误无裁决依据 | 逻辑 | 严格复审发现两份治理文件对"测试文件笔误能否自动改"给出相反指令 | HARNESS 自动级触发条件排除 §2.3 受保护产物（harness-test-suite 测试文件、EVIDENCE 历史证据），须按 §2.3 走 AskUserQuestion |
| 2026-08-16 | EV-013 | 契约变更清理无失效处理：仅规定"所有受影响消费者完成后删行"，未覆盖消费者被"已跳过/已阻塞"永不完成的情形，增量行无限累积违反精简铁律 | 逻辑 | 严格复审发现契约变更节缺孤儿行清理规则 | SKILL 契约变更清理 + 模板压缩规则增"消费者已跳过/已阻塞且不再消费该 delta 时亦删除或标注失效" |
| 2026-08-16 | EV-014 | 元循环确认偏差残角：Self-Harness「无新证据即停」由主代理主观判定，可能正受其确认偏差影响而过早停，与 EV-007（主代理既提又验）同源 | 逻辑 | 第三轮深挖发现 EV-007 仅修"提案验证"环节，未封"停止条件"环节的主观判定盲区 | SKILL 停止条件「无新证据」行增补：判定前须先调三轴/方案审查快速模式（反对者子代理）独立盲区审计，仅独立审计者亦未发现新弱点方可停；task-07 守卫该约束不被意外删 |

> 待办提议（非失败模式，不编号）：发现10——可将兄弟 skill「文件守卫」的 Markdown 守卫规则（UTF-8 无 BOM / 无重复标题 / 代码块标注 / 紧凑表格）固化为 harness-test-suite 新回归任务，防 skill 自身 md 退化。

## 压缩规则

超过 20 条记录时，删除最早 5 条非活跃证据；活跃证据始终保留。
