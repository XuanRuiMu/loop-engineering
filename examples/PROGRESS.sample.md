# PROGRESS.md — sample

> A filled-in, compact `PROGRESS.md` produced by Loop Engineering for a real refactor.
> 这是循环工程在一次真实重构中产出的「精简版」PROGRESS.md 样例。

## 元信息
- 目标：重构认证模块，按依赖顺序逐个功能点推进
- 停止条件：全部功能点状态=已完成，且全量测试通过、lint 零报错、构建成功
- 熔断上限：总循环 30 轮；单问题修复 5 次；补位上限 3
- 创建时间：2026-08-17

## 范围边界
- 做什么：登录接口、令牌刷新、注销、会话存储抽象
- 不做什么：权限模型重写、第三方 OAuth 接入
- 禁止触碰：src/db/schema.sql、src/config/*.secret

## 待处理功能点
| ID | 描述 | 验收标准 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| FP-01 | 抽象会话存储接口 | 新增 SessionStore 接口 + 内存实现，测试覆盖 | — | 已完成 |
| FP-02 | 登录接口迁移到新存储 | 登录成功/失败用例通过，lint 零报错 | FP-01 | 进行中 |
| FP-03 | 令牌刷新 | 刷新成功/过期用例通过 | FP-02 | 待开始 |
| FP-04 | 注销清理会话 | 注销后会话不可恢复用例通过 | FP-02 | 待开始 |

## 契约变更（唯一允许的进度外记录）
- FP-01 → 新增 `SessionStore.get/set/del(sessionId, data)`，影响 FP-02/03/04 的会话读写调用方

## 已完成（仅保留一行摘要）
- FP-01：会话存储抽象完成，测试 9/9 通过，lint 零报错

## 当前决策
- FP-02 派发前已验证 FP-01 基线编译通过；已将「SessionStore 契约 delta」注入 FP-02 子代理提示词。

## 元循环摘要
- Self-Harness：本轮发现 1 个弱点（子代理偶发返回完整日志），已自动补强提示词模板的「仅返回摘要」检查项并通过回归。
